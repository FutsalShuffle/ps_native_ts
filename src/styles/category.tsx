import {
    StyleSheet
} from 'react-native';

const MainStyles = StyleSheet.create({
    item: {
      width: '100%',
      padding: 10,
      borderWidth: 1,
      borderRadius: 10,
      borderTopWidth: 0,
      marginTop: 10,
    },
    text: {
      fontSize: 24,
      textAlign: 'center',
    },
});

export default MainStyles;